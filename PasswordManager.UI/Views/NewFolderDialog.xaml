<Window x:Class="PasswordManager.UI.Views.NewFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager.UI.Views"
        mc:Ignorable="d"
        Style="{StaticResource DialogWindowStyle}"
        Title="Create new folder">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Folder name:" Grid.Column="0" Style="{StaticResource DialogTextBlockStyle}"/>
            <TextBox x:Name="FolderNameTextBox" Grid.Column="1" Style="{StaticResource DialogTextBoxStyle}"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Add" Style="{StaticResource DialogButtonOK}"
                    Margin="0,0,20,0" Click="AddButton_Click"/>
            <Button Content="Cancel" Style="{StaticResource DialogButtonCancel}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
