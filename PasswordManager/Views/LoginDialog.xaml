<Window x:Class="PasswordManager.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager.Views"
        mc:Ignorable="d"
        Style="{DynamicResource DialogWindowStyle}"
        WindowStartupLocation="CenterScreen"
        Title="Enter your password"
        ShowInTaskbar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Master password:" Grid.Column="0" Style="{DynamicResource DialogTextBlockStyle}"/>
            <PasswordBox x:Name="MasterPasswordBox" Grid.Column="1" Style="{DynamicResource DialogPasswordBoxStyle}"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0 20 0 0">
            
            <Button Content="Login" Style="{DynamicResource DialogButtonOK}" Click="Login_Click"/>

            <Button Content="Reset Password" Style="{DynamicResource DeleteConfirmationButtonStyle}"
                    Margin="0 0 20 0" Click="ResetPassword_Click" Width="170"/>

            <Button Content="Cancel" Style="{DynamicResource DialogButtonCancel}" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
